import {
  cvToJSON,
  deserializeCV,
  serializeCV,
  ClarityValue,
} from "@stacks/transactions";

export async function readContract({
  contractAddress,
  contractName,
  functionName,
  functionArgs = [],
}) {}
